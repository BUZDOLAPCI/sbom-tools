# Server Configuration
SERVER_NAME=sbom-tools
SERVER_VERSION=1.0.0

# Transport Configuration
# Options: stdio, http
TRANSPORT=stdio

# HTTP Transport Settings (only used when TRANSPORT=http)
HTTP_PORT=3000
HTTP_HOST=127.0.0.1

# Logging
# Options: debug, info, warn, error
LOG_LEVEL=info

# Tool Metadata (appears in generated SBOMs)
TOOL_VENDOR=Dedalus Labs
TOOL_NAME=sbom-tools
